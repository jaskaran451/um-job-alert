const form=document.querySelector('#subscription-form');if(form){const input=document.querySelector('#keyword-input'),list=document.querySelector('#keyword-list'),add=document.querySelector('#add-keyword'),keywords=[];function render(){list.replaceChildren();keywords.forEach((k,i)=>{const tag=document.createElement('span');tag.className='tag';tag.append(k);const x=document.createElement('button');x.type='button';x.textContent='×';x.setAttribute('aria-label',`Remove ${k}`);x.onclick=()=>{keywords.splice(i,1);render()};tag.append(x);list.append(tag)})}function addKeyword(value){const k=value.trim().replace(/\s+/g,' ').replace(/^,+|,+$/g,'');if(k.length<2||k.length>40||keywords.length>=8)return;if(!keywords.some(v=>v.toLowerCase()===k.toLowerCase()))keywords.push(k);input.value='';render()}add.onclick=()=>addKeyword(input.value);input.onkeydown=e=>{if(e.key==='Enter'||e.key===','){e.preventDefault();addKeyword(input.value)}};document.querySelectorAll('[data-keyword]').forEach(b=>b.onclick=()=>addKeyword(b.dataset.keyword));const all=form.querySelector('input[value="all"]'),others=[...form.querySelectorAll('input[name="role_type"]')].filter(x=>x!==all);all.onchange=()=>{if(all.checked)others.forEach(x=>x.checked=false)};others.forEach(x=>x.onchange=()=>{if(x.checked)all.checked=false});form.onsubmit=async e=>{e.preventDefault();['email','preferences','consent'].forEach(x=>document.querySelector(`#${x}-error`).textContent='');const message=document.querySelector('#form-message'),button=form.querySelector('.primary');message.className='message';message.textContent='';button.disabled=true;const payload={email:document.querySelector('#email').value,role_types:[...form.querySelectorAll('input[name="role_type"]:checked')].map(x=>x.value),keywords,consent:document.querySelector('#consent').checked,company:document.querySelector('#company').value};try{const response=await fetch('/api/subscriptions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}),result=await response.json();if(!response.ok){Object.entries(result.errors||{}).forEach(([k,v])=>{const el=document.querySelector(`#${k}-error`);if(el)el.textContent=v});throw new Error(result.message||'Could not save your alert.')}message.className='message success';message.textContent=`Alert created. We’ll email ${result.email} when a new title matches.`}catch(error){if(!message.textContent){message.className='message error-box';message.textContent=error.message}}finally{button.disabled=false}}}